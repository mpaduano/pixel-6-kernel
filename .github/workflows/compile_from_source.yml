name: compile_from_source

on:
  workflow_dispatch:

concurrency:
  group: compile_from_source-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 360

    env:
      DEVICE: bluejay
      KERNEL_BRANCH: common-android14-6.1-2026-03
      MANIFEST_URL: https://android.googlesource.com/kernel/manifest

      KERNEL_DIR: bluejay-kernel
      DIST_DIR: dist

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install deps + repo
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends \
            bc bison build-essential ca-certificates ccache clang cpio curl file flex git lld llvm libelf-dev make ninja-build openssl pahole pkg-config python3 python3-pip rsync unzip xz-utils zstd binutils binutils-dev
          
          sudo install -d -m 0755 /usr/local/bin
          sudo curl -fsSL -o /usr/local/bin/repo https://storage.googleapis.com/git-repo-downloads/repo
          sudo chmod 0755 /usr/local/bin/repo

      - name: Cache repo (.repo only)
        uses: actions/cache@v4
        with:
          path: android-kernel/.repo
          key: repo-${{ runner.os }}-${{ env.KERNEL_BRANCH }}-${{ hashFiles('.github/workflows/compile_from_source.yml') }}
          restore-keys: |
            repo-${{ runner.os }}-${{ env.KERNEL_BRANCH }}-

      - name: repo init + sync
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${GITHUB_WORKSPACE}/${KERNEL_DIR}"
          cd "${GITHUB_WORKSPACE}/${KERNEL_DIR}"

          if [ ! -d ".repo" ]; then
            /usr/local/bin/repo init -u "${MANIFEST_URL}" -b "${KERNEL_BRANCH}"
          fi

          /usr/local/bin/repo sync -c -j"$(nproc)" --force-sync --no-clone-bundle --no-tags --optimized-fetch --prune

      - name: Patch gki_defconfig
        shell: bash
        run: |
          set -euo pipefail
          cd "${GITHUB_WORKSPACE}/${KERNEL_DIR}"
          
          DEFCONFIG="common/arch/arm64/configs/gki_defconfig"
          
          python3 - <<'PY'
          from pathlib import Path
          p = Path("common/arch/arm64/configs/gki_defconfig")
          lines = p.read_text().splitlines(True)
          
          remove_keys = {"CONFIG_BT_HCIBTUSB", "CONFIG_BT_HCI", "CONFIG_USB", "CONFIG_USB_SUPPORT"}
          cleaned = [ln for ln in lines if not any(ln.startswith(k + "=") for k in remove_keys)]
          
          insert_line = "CONFIG_BT_HCIBTUSB=m\n"
          
          idx = next((i + 1 for i, ln in enumerate(cleaned) if ln.startswith("CONFIG_BT_HIDP=")), None)
          if idx is None:
              last_bt = next((i for i, ln in reversed(list(enumerate(cleaned))) if ln.startswith("CONFIG_BT_")), None)
              idx = (last_bt + 1) if last_bt is not None else len(cleaned)
          
          cleaned.insert(idx, insert_line)
          out = "".join(cleaned)
          if not out.endswith("\n"): out += "\n"
          p.write_text(out)
          PY

      - name: Prepare vendor ramdisk symlinks
        shell: bash
        run: |
          set -euo pipefail
          RAMDISK_DIR="${GITHUB_WORKSPACE}/${KERNEL_DIR}/prebuilts/boot-artifacts/ramdisks"
          mkdir -p "${RAMDISK_DIR}/vendor_ramdisk"
          cp -f "${GITHUB_WORKSPACE}/vendor_ramdisk00" "${RAMDISK_DIR}/vendor_ramdisk-oriole.img"
          ln -sf "vendor_ramdisk-oriole.img" "${RAMDISK_DIR}/oriole.img"
          ln -sf "../vendor_ramdisk-oriole.img" "${RAMDISK_DIR}/vendor_ramdisk/oriole.img"

      - name: Patch Bazel module export list
        shell: bash
        run: |
          set -euo pipefail
          cd "${GITHUB_WORKSPACE}/${KERNEL_DIR}/common"
          
          echo "Bypassing Buildozer. Force-injecting modules into BUILD.bazel via Python..."
          python3 - <<'PY'
          import re
          with open("BUILD.bazel", "r") as f:
              content = f.read()
          
          # Safely inject the required Realtek/USB modules into every module_outs array
          replacement = "\\1\n    \"drivers/bluetooth/btintel.ko\",\n    \"drivers/bluetooth/btusb.ko\",\n    \"drivers/bluetooth/btrtl.ko\","
          patched = re.sub(r"(module_outs\s*=\s*\[)", replacement, content)
          
          with open("BUILD.bazel", "w") as f:
              f.write(patched)
          PY
          echo "Raw text patch applied successfully."

      - name: Build dist (Kleaf / Bazel)
        shell: bash
        run: |
          set -euo pipefail
          cd "${GITHUB_WORKSPACE}/${KERNEL_DIR}"
          rm -rf out/bazel || true
          export BAZEL_OUTPUT_USER_ROOT="/tmp/bazel_user_root_${GITHUB_RUN_ID}"
          mkdir -p "${GITHUB_WORKSPACE}/${DIST_DIR}"

          TARGET="$(python3 -c '
          import re, sys
          b = open("common/BUILD.bazel", errors="ignore").read()
          m = re.search(r"name\s*=\s*\"(kernel_aarch64[^\"]*_dist)\"", b) or re.search(r"name\s*=\s*\"([^\"]*_dist)\"", b)
          if m: print("//common:" + m.group(1)); sys.exit(0)
          sys.exit(1)
          ')" || TARGET="//common:kernel_aarch64_dist"
          
          ./tools/bazel run "${TARGET}" -- --dist_dir="${GITHUB_WORKSPACE}/${DIST_DIR}" 2>&1 | tee "${GITHUB_WORKSPACE}/build.log"

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ env.DEVICE }}
          path: |
            dist/**
            build.log
          if-no-files-found: warn
